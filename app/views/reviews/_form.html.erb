<%= form_for [@school, @teacher, @review] do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this @review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :author, :value => current_user.first_name %><br>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %><br>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %><br>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
